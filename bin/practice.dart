
import 'package:practice/variable/variable.dart';

main(List<String> arguments) async {
  variable();
  // await for (final estimate in computePi().take(100)) {
  //   print('π ≅ $estimate');
  // }
}